#!/bin/bash

# Set background opacity (adjust value as needed)
kitty @ set-background-opacity 1.0

# Change color scheme dynamically (replace with your theme file)
kitty @ set-colors -a "$HOME/.config/kitty/dark-theme.auto.conf"
